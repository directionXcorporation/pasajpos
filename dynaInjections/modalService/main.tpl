<script type="text/ng-template" id="openPageInModal0.tpl">
    <md-dialog aria-label="Details" flex>
    	<md-toolbar flex class="page-toolbar toolbar">
    		<div class="md-toolbar-tools">
    			<span flex translate>{{$ctrl.title}}</span>
    			<md-button class="md-icon-button" data-ng-if="$ctrl.showActionButtons" data-ng-click="$ctrl.clickToSave()" id="okButon_modal0">
    				<md-icon aria-label="Save Modal" md-font-icon="check_circle">check_circle</md-icon>
    			</md-button>
    			<md-button class="md-icon-button" data-ng-click="$ctrl.clickToClose()" id="closeButon_modal0" id="closeButon_modal0">
    				<md-icon aria-label="Close Modal" md-font-icon="close">close</md-icon>
    			</md-button>
    		</div>
    	</md-toolbar>
    	<md-dialog-content flex="noshrink">
    		<div ui-view="modal0"></div>
    	</md-dialog-content>
    </md-dialog>
</script>

<script type="text/ng-template" id="openPageInModal1.tpl">
    <md-dialog aria-label="Details" flex>
    	<md-toolbar flex class="page-toolbar toolbar">
    		<div class="md-toolbar-tools">
    			<span flex translate>{{$ctrl.title}}</span>
    			<md-button class="md-icon-button" data-ng-if="$ctrl.showActionButtons" data-ng-click="$ctrl.clickToSave()" id="okButon_modal1">
    				<md-icon aria-label="Save Modal" md-font-icon="check_circle">check_circle</md-icon>
    			</md-button>
    			<md-button class="md-icon-button" data-ng-click="$ctrl.clickToClose()" id="closeButon_modal1" id="closeButon_modal1">
    				<md-icon aria-label="Close Modal" md-font-icon="close">close</md-icon>
    			</md-button>
    		</div>
    	</md-toolbar>
    	<md-dialog-content flex="noshrink">
    		<div ui-view="modal1"></div>
    	</md-dialog-content>
    </md-dialog>
</script>

<script type="text/ng-template" id="openPageInModal2.tpl">
    <md-dialog aria-label="Details" flex>
    	<md-toolbar flex class="page-toolbar toolbar">
    		<div class="md-toolbar-tools">
    			<span flex translate>{{$ctrl.title}}</span>
    			<md-button class="md-icon-button" data-ng-if="$ctrl.showActionButtons" data-ng-click="$ctrl.clickToSave()" id="okButon_modal2">
    				<md-icon aria-label="Save Modal" md-font-icon="check_circle">check_circle</md-icon>
    			</md-button>
    			<md-button class="md-icon-button" data-ng-click="$ctrl.clickToClose()" id="closeButon_modal2" id="closeButon_modal2">
    				<md-icon aria-label="Close Modal" md-font-icon="close">close</md-icon>
    			</md-button>
    		</div>
    	</md-toolbar>
    	<md-dialog-content flex="noshrink">
    		<div ui-view="modal2"></div>
    	</md-dialog-content>
    </md-dialog>
</script>

<script type="text/ng-template" id="openPageInModal3.tpl">
    <md-dialog aria-label="Details" flex>
    	<md-toolbar flex class="page-toolbar toolbar">
    		<div class="md-toolbar-tools">
    			<span flex translate>{{$ctrl.title}}</span>
    			<md-button class="md-icon-button" data-ng-if="$ctrl.showActionButtons" data-ng-click="$ctrl.clickToSave()" id="okButon_modal3">
    				<md-icon aria-label="Save Modal" md-font-icon="check_circle">check_circle</md-icon>
    			</md-button>
    			<md-button class="md-icon-button" data-ng-click="$ctrl.clickToClose()" id="closeButon_modal3" id="closeButon_modal3">
    				<md-icon aria-label="Close Modal" md-font-icon="close">close</md-icon>
    			</md-button>
    		</div>
    	</md-toolbar>
    	<md-dialog-content flex="noshrink">
    		<div ui-view="modal3"></div>
    	</md-dialog-content>
    </md-dialog>
</script>

<script type="text/ng-template" id="openPageInModal4.tpl">
    <md-dialog aria-label="Details" flex>
    	<md-toolbar flex class="page-toolbar toolbar">
    		<div class="md-toolbar-tools">
    			<span flex translate>{{$ctrl.title}}</span>
    			<md-button class="md-icon-button" data-ng-if="$ctrl.showActionButtons" data-ng-click="$ctrl.clickToSave()" id="okButon_modal4">
    				<md-icon aria-label="Save Modal" md-font-icon="check_circle">check_circle</md-icon>
    			</md-button>
    			<md-button class="md-icon-button" data-ng-click="$ctrl.clickToClose()" id="closeButon_modal4" id="closeButon_modal4">
    				<md-icon aria-label="Close Modal" md-font-icon="close">close</md-icon>
    			</md-button>
    		</div>
    	</md-toolbar>
    	<md-dialog-content flex="noshrink">
    		<div ui-view="modal4"></div>
    	</md-dialog-content>
    </md-dialog>
</script>